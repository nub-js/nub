name: Sandbox metadata and tool-bundle costs
on:
  push:
    branches: [sandbox-self-proc-costs]
    paths:
      - crates/nub-sandbox/**
      - .github/workflows/sandbox-self-proc-costs.yml
permissions:
  contents: read
jobs:
  costs:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6
        with:
          shared-key: sandbox-self-proc-release
      - name: Binding current and baseline artifacts
        run: |
          mkdir -p reports
          git rev-parse HEAD > reports/environment.txt
          uname -a >> reports/environment.txt
          rustc -Vv >> reports/environment.txt
          git worktree add --detach "$RUNNER_TEMP/sandbox-baseline" 45d5be8b26b456c15c094b107cb6cd725bce8032
          cp crates/nub-sandbox/tests/self_proc_costs.rs "$RUNNER_TEMP/sandbox-baseline/crates/nub-sandbox/tests/self_proc_costs.rs"
          sha256sum crates/nub-sandbox/tests/self_proc_costs.rs >> reports/environment.txt
      - name: Measuring current policy costs and enforcement
        run: |
          set -o pipefail
          cargo test -p nub-sandbox --release --test self_proc_costs metadata_costs -- --ignored --nocapture --test-threads=1 2>&1 | tee reports/current.log
          cargo test -p nub-sandbox --release --test self_proc -- --nocapture --test-threads=1 2>&1 | tee reports/enforcement.log
      - name: Measuring the identical workload on the preceding engine
        env:
          SELF_PROC_BASELINE: '1'
        run: |
          set -o pipefail
          cd "$RUNNER_TEMP/sandbox-baseline"
          cargo test -p nub-sandbox --release --test self_proc_costs metadata_costs -- --ignored --nocapture --test-threads=1 2>&1 | tee "$GITHUB_WORKSPACE/reports/baseline.log"
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22.18.0'
      - name: Measuring retained tool operations against plain controls
        run: |
          set -o pipefail
          node scripts/sandbox-tool-fixtures.mjs > reports/provision.log 2>&1
          cp .sandbox-tool-fixtures/matrix.json reports/tool-matrix.json
          export NUB_SANDBOX_TOOL_MATRIX_FILE="$GITHUB_WORKSPACE/reports/tool-matrix.json"
          cargo test -p nub-sandbox --release --test tool_functionality linux_tool_bundle_release_costs -- --ignored --nocapture --test-threads=1 2>&1 | tee reports/tool-costs.log
      - name: Measuring the identical tool workload before event-driven waits
        run: |
          set -o pipefail
          git worktree add --detach "$RUNNER_TEMP/sandbox-tool-baseline" 177e3a51c3f8f592507993b126fc9834b2d577b4
          cp crates/nub-sandbox/tests/tool_functionality.rs "$RUNNER_TEMP/sandbox-tool-baseline/crates/nub-sandbox/tests/tool_functionality.rs"
          sha256sum crates/nub-sandbox/tests/tool_functionality.rs >> reports/environment.txt
          cd "$RUNNER_TEMP/sandbox-tool-baseline"
          cargo test -p nub-sandbox --release --test tool_functionality linux_tool_bundle_release_costs -- --ignored --nocapture --test-threads=1 2>&1 | tee "$GITHUB_WORKSPACE/reports/tool-costs-before-wait.log"
      - name: Preserving performance artifacts
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: self-proc-costs-linux
          path: reports
